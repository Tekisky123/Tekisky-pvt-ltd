import Testimonials from "@/components/Testimonials";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Testimonials />
    </div>
  );
};

export default page;
